# AGENTS.md

本文件是本仓库中 AI 编码代理的项目级工作说明。它记录需要反复遵守、且不应靠猜测决定的项目事实与边界；详细内容以 `docs/` 中的文档为准。

## ⚠️ 当前阶段

**项目处于实现阶段（2026-09-25 起）。第 13 章 9 阶段（0–8）中：阶段 0–4 已完成（底座 / 接触层 / 判定层 / 编排层 / 接入层——引擎已监听 8090，`npm start` 可跑，事件流与证据链同源，Gate 3 对外接口清单已登记为基线文档）**，见 [阶段 4 变更记录](docs/变更记录/2026-09-25-阶段4-接入层.md)。下一步是**阶段 5（理解层：自然语言 → 结构化意图）**。此前产出的可运行实现已整体降级至 `_prototype/`（不得在其上继续开发）。

- 交付物：`docs/` 下的基线文档、设计方案、变更记录、研发规范；实现期代码交付物（当前为 `orchestrator/`）
- 非交付物：`_prototype/`（降级件，仅保全实测结论，不得在其上继续开发、方案不得引用其结构）
- 设计方案 **13 章已定稿**，各章待决项已汇总（见 `docs/设计方案/2026-09-25-方案待决项决策汇总.md`），并已完成**架构一审、二审、三审（均 2026-09-25）**：
  - [一审记录](docs/变更记录/2026-09-25-架构一审-方案评审与回填修订.md)：新增 J1–J7
  - [二审记录](docs/变更记录/2026-09-25-架构二审-实现前置评审与回填修订.md)：新增 K1–K6 与开工 Gate
  - [三审记录](docs/变更记录/2026-09-25-架构三审-开工Gate关闭与实测回填.md)：**J1–J7/K1–K6 全部拍板、Gate 2 实测（J3/J4/D10/C19 等）全部完成并回填，开工 Gate 全部关闭**
- **权威值口径**：各章末尾待决清单按规范保留决策前状态；实际值（步数 60、三段区间、ACTIVE 有效集、候选 K=5 等）以决策汇总与三审记录为准；这些值已落进 `orchestrator/config/constants.yaml`

**任何新会话不得假设前端、混沌模块、L1–L5 各层已实现**（阶段 0 只交付了口径/证据/配置/自检四类底座，按第 02 章 §九 7 不预建空层）；也**不得假设 `_prototype/` 的结构可用作依据**。实现新功能前先跑 `npm run check`（8 条结构自检），自检不过不允许继续开发。

## 工作原则

- 不确定时不要编造约定、接口或业务规则。先阅读 `docs/基线文档/`、`README.md` 与相关文档，再作决定。
- **用户要方案时，交付方案。** 方案写细是优点；替用户把实现也做完是越界（见 `docs/变更记录/2026-09-24-交付物越界与降级处置.md`）。
- 歧义指令先问。尤其"架子""原型""先弄一版"这类主语宾语不明确的词，先确认再动手。
- 功能开发遵循「先文档、后实现、再验证」。涉及接口、架构或重要业务行为的变动，先更新对应文档；较大变更记录到 `docs/变更记录/`。
- 改动 `mock-legacy/` 之前**必须先读** `docs/基线文档/存量系统改造说明.md`。该目录默认视为**不可修改的第三方系统**。
- 不在代码、文档或提交中添加密码、JWT 密钥、Token 或其他真实凭据。

## 项目概览

**面向存量业务系统的 AI 任务编排平台**（校园教室借用为验证场景）。

一句话：用自然语言交互，由自研状态机 Agent 自动编排、调度存量业务系统的多个 API，完成跨接口、跨权限域的业务，并在接口抖动/超时/脏返回下保持鲁棒；**不修改存量系统的业务代码**。

目标分层（设计中，见 `docs/设计方案/2026-09-24-系统设计方案/` 第 02 章）：

```text
对话前端  →  编排引擎（自研核心）  →  防腐网关  →  存量业务系统（mock-legacy，不可改）
```

运行端口约定（存量系统已实测，其余为设计目标）：

| 服务 | 端口 | 说明 |
|---|---|---|
| 存量系统后端 | `8080` | mock-legacy，保持其默认端口不改，**已可运行** |
| 编排引擎 | `8090` | 已决策（决策汇总 A2），尚未实现 |
| 对话前端（dev） | `5173` | Vite dev server（生产形态用 Vite 预览独立进程，已决策 A8），尚未实现 |
| Redis | `6379` | 存量系统依赖 |
| MariaDB | `3306` | 存量系统依赖 |

先阅读（按此顺序）：

- `docs/基线文档/存量系统改造说明.md`：**事实地基**。被测存量系统改了什么、为什么、什么坚决不改；实测记录与协议异构点。
- `docs/基线文档/业务边界与责任域.md`：**管什么 / 不管什么**，三个边界决策点与裁决原则。
- `docs/基线文档/智能体设计.md`：**核心**。从「现实事实 → 边界 → 不变量 I1–I6 → 核心命题 P1–P6 → 系统模型」的完整推演。
- `docs/基线文档/对外接口清单.md`：**编排引擎对外 API 与事件 schema（Gate 3 登记件）**。
- `docs/设计方案/2026-09-24-系统设计方案/README.md`：方案总纲，交付物定义、颗粒度标准、章节目录。
- `docs/设计方案/2026-09-24-系统设计方案/01-存量接口纳管方案.md`：纳管判据与逐接口设计。
- `docs/设计方案/2026-09-25-方案待决项决策汇总.md`：**待决项决策的唯一权威来源**（§10.1 三审实测回填、§十三/§十四 三审拍板注、§十五 三审定稿项）。
- `docs/变更记录/2026-09-25-架构一审-方案评审与回填修订.md`：**架构一审**——全方案评审结论、回填修订清单、新预见的业务边界。
- `docs/变更记录/2026-09-25-架构二审-实现前置评审与回填修订.md`：**架构二审**——实现前置评审结论、二次回填清单、开工 Gate 清单。
- `docs/变更记录/2026-09-25-架构三审-开工Gate关闭与实测回填.md`：**架构三审（最新状态）**——拍板与实测结论、口径统一清单、**Gate 关闭声明**。**实现前必读。**
- `docs/研发规范/文档驱动开发规范.md`：文档体系的分层、命名、维护规则与五步闭环。

## 仓库结构与边界

```text
docs/                     文档体系（交付物，见 docs/README.md）
  基线文档/               长期维护的规则文档（不带日期）
  设计方案/               带日期的设计过程文档（主方案 13 章已定稿，含一/二/三审回填）
  变更记录/               带日期的过程记录
  研发规范/               写作与协作规范
orchestrator/             编排引擎（阶段 0–4 已完成：底座 + 接触层 + 判定层 + 编排层 + 接入层
                          （8090，对外接口见 docs/基线文档/对外接口清单.md）；详见 orchestrator/README.md）
mock-legacy/              被测存量系统（第三方，零侵入，**已可运行**）
frontend/                 对话前端（**尚未实现**，空占位）
deploy/                   部署脚本与配置（存量系统启动/复位脚本）
_prototype/               降级件（非交付物，冻结，不得继续开发）
  orchestrator/           2026-09-24 产出的可运行实现（已降级）
  docs/                   为上述代码写的说明书（已降级）
```

**`_prototype/` 的三条规矩**（见 `_prototype/README.md`）：
1. 方案文档不得引用其代码结构、模块命名、文件划分
2. 不得在其上继续开发
3. 它随时可以被整份删除（有长期价值的事实结论必须已落在 `docs/` 里）

## 核心设计原则

1. **零侵入**：不修改 `mock-legacy/` 源码。所有协议差异由网关吸收，不下沉到引擎或存量系统。
2. **脏必须是真的**：异构来自存量系统本身的客观现状，不靠人为编造。发现新的异构点时，登记到文档，不是去改存量系统把它抹平。
3. **能用只读接口查证，就不要靠猜**：存量系统的写入接口不保证幂等。任何重试前必须先走幂等查证路径。
4. **补偿不保证成功**：补偿动作可能返回冲突。补偿失败必须落到「人工介入」终态并如实上报，禁止静默吞掉。
5. **校验在调用之前**：业务前置条件校验发生在请求到达存量系统之前。

## 关键不变量

以下不变量来自 `docs/基线文档/智能体设计.md` §四，是设计与实现必须维护的硬约束：

- **I1**：任何有副作用的调用发出前，其前置条件所依赖的事实必须已被收集且已判定为满足。
- **I2**：对同一个业务意图（身份 + 资源 + 时段），存量系统中至多存在一条有效记录。
- **I3**：每次调用结束后，系统必须能给出唯一确定的结论：成功 / 失败 / 不确定；"不确定"必须被继续收敛，不得悬空。
- **I4**：系统必须能回答"为什么做了这一步 / 为什么拒绝 / 依据什么事实"。
- **I5**：任何一次调用只能以具备该接口权限的最小身份发出；无权限时不重试、不降级猜测。
- **I6**：产生了副作用而任务未完成时，副作用必须被补偿或显式登记为不可补偿。

**⚠️ 已被实测推翻的假设（勿再沿用）**：早期文档曾写「存量系统的创建预约接口不去重，重复提交会真实产生多条记录」——**这是错的**。实测结论：
- 预约接口有时间重叠冲突检测，冲突时返回 `HTTP 200 + body.code = 409`；6 路并发也只落库 1 条。
- 真正的问题不是"会写脏"，而是 **409 是歧义信号**：它无法区分「你自己刚才已经提交成功了」和「别人占了」。因此 409 既不能当成功、也不能当失败，必须先查证收敛。
- 幂等守卫的目标随之改变：不是"防重复写入"，而是**"把歧义信号收敛成确定的成功/失败"**。
- 另：`min_capacity` 缺失时返回业务码 500 而非 400；`keyword` 参数语义模糊，不可用于精确过滤，查证须拉全量再内存比对。

## 命名与规模

- Node 文件与目录使用小写连字符（如 `state-machine/execute-task.js`）。
- 类/构造函数 PascalCase，函数与变量 camelCase，常量 `UPPER_SNAKE_CASE`。
- 文档命名遵循 `docs/研发规范/文档驱动开发规范.md`：基线文档不带日期，变更记录与设计方案带 `YYYY-MM-DD-` 前缀。
- 单个文件 ≤ 500 行；接近 400 行即考虑拆分。（实现阶段适用）

## 验证命令

按改动范围运行最小充分验证，并如实报告未执行的验证：

```bash
# 编排引擎（阶段 0–4：含可运行服务）
cd orchestrator
npm install        # 首次
npm test           # 全部测试（112 个；含真实 HTTP/SSE 的接入层测试）
npm run check      # 8 条结构自检 + 4 条约束检查——改动后必跑，不过不允许继续开发
npm start          # 启动引擎（8090；前置：存量系统/Redis 就绪；凭证走环境变量）：
#   ORCH_LEGACY_ADMIN_PASSWORD=admin ORCH_LEGACY_TEACHER_PASSWORD=233 npm start
# 真实冒烟（只读）：node scripts/smoke-contact.mjs（需同上环境变量）
```

```bash
# 存量系统（需 MariaDB 与 Redis 已启动，首次需建库导数据，见部署手册）
cd mock-legacy/backend
JAVA_HOME="/c/Program Files/Microsoft/jdk-17.0.8.7-hotspot" mvn -DskipTests compile
JAVA_HOME="/c/Program Files/Microsoft/jdk-17.0.8.7-hotspot" mvn spring-boot:run
```

- 存量系统必须使用 **JDK 17** 编译运行；本机 PATH 上的默认 Java 是 24，与 Lombok 1.18.30 不兼容，**不要**用默认 `java`/`mvn`。
- 存量系统依赖 Redis：Redis 未启动时，登录后的接口会一律返回 401。

**前端、混沌模块的验证命令暂不适用**——尚未实现，随阶段 6/7 的实现补充。

## 完成标准

提交一个改动前，确认：需求范围已实现；分层与接口契约一致；零侵入原则没有被破坏（`mock-legacy/` 无未登记改动）；核心业务规则（幂等、校验、补偿）没有被绕过；相关文档已更新；并已运行适合该改动的编译、测试或端到端验证。完成说明应包含改动内容、验证结果与尚存的不确定性。

**文档阶段的完成标准**：方案文档需满足总纲中的三条颗粒度标准（写到"能照着做"、每条决定附四件套、不留隐含前提）；每条关键决定附可行性评估点。
