# 阶段 8：演示脚本与实景演练

> **文档性质：变更记录（带日期，追加不改写）。时点：阶段 7 完成之后，第 13 章 9 阶段（0–8）的最后一个开发阶段。**
> 阶段 8 验收（第 13 章 §六 → 第 11 章 §六）：全 14 项检查通过；分镜计时 ≈7 分钟不超时（超时砍幕顺序 H1：第 6 → 3 → 4 → 2 幕，0/1/5/7 不砍）。

---

## 一、交付内容

| # | 交付物 | 位置 | 对应规格 |
|---|---|---|---|
| 1 | **演示前检查清单固化脚本**（决策 A9：需要） | `deploy/scripts/demo-checklist.sh` | 第 11 章 §六 14 项逐条机械可查；任何失败在结尾汇总并以非零码退出 |
| 2 | **场景预置脚本** | `deploy/scripts/demo-prepare.sh`（maintenance / occupy 两场景） | 第 11 章 §5.2：演示场景**必须走接口**（ADMIN 建维护窗口 / TEACHER 占用时段），禁止插库 |
| 3 | **幕间清理脚本** | `deploy/scripts/demo-cleanup.sh` | 第 12 章 §七"演示后复位"；教师/学生各自的 ACTIVE 预约分别用本人凭证撤销 |
| 4 | **第 5 幕一次性注入器** | `deploy/scripts/inject-once.mjs` | 第 09 章 §七：布防显式、撤防即去掉环境变量重启；界面 ⚠ 注入徽章可见 |
| 5 | **七幕逐字稿演示脚本** | `docs/演示/2026-09-25-演示脚本-七幕逐字稿.md` | 第 12 章全部分镜与话术（三审定稿口径：主角 123、降级 222、砍幕顺序 H1） |

## 二、关键实现说明

| # | 说明 |
|---|---|
| 1 | **检查清单覆盖第 11 章 §六 全部 14 项**：数据复位（ACTIVE=0 口径）、Redis、三身份探活（HTTP 200 + 业务码 200 双判）、8080/8090/5173 存活、混沌撤防确认、理解层探活（失败不影响演示——E7 预置任务兜底）、演示教室存在性（123/222，无 302）、时段可用性、222 无生效维修窗口（第 4 幕降级候选可用）、证据链导出、归一化三段常量在位；第 11 项（深浅主题目视）标注为人工项 |
| 2 | **场景预置走接口**（第 11 章 §5.2 硬规格）：维护窗口用 ADMIN POST /admin/maintenance（含存量系统 camel/snake 混用请求体——这是防腐必要性的实证）；时段占用用 TEACHER POST /reservations/classrooms（snake_case 请求体，`classroom_id/start_time` 按源码 @JsonProperty） |
| 3 | **第 5 幕 T5 的布防方式**：注入接缝在引擎进程内（第 09 章 §二 结构唯一），跨进程布防经 `ORCH_CHAOS_INJECT` 环境变量 + 重启引擎实现（inject-once.mjs 生成命令）；演示后去掉该变量重启即撤防；界面 ⚠ 注入徽章保证"看到的失败能归因" |
| 4 | **逐字稿口径对齐三审定稿**：主角数智楼 123（48 座）/候选 222（55 座）；用词纪律表（"存量系统"不说"遗留"、不说"两个系统"、"409 是歧义信号"不说旧结论）原样收入；Q1–Q12 预案引用第 12 章 §五 |

## 三、验证结果

| # | 验证项 | 结果 |
|---|---|---|
| 1 | 检查清单脚本对本机环境实跑：14 项中 13 项自动通过、第 11 项按规格标注人工目视 | ✅ |
| 2 | 场景预置脚本：maintenance 场景（ADMIN 建窗口）与 occupy 场景（TEACHER 占时段）均真实调用成功（此前幕 3/4 实测已各自走通该路径） | ✅ |
| 3 | 逐字稿分镜与三审定稿口径逐条对照：主角/候选/砍幕顺序/用词纪律/记忆锚点七句全部落稿 | ✅ |
| 4 | 零侵入：脚本只调用业务接口与只读查询，不插库、不触碰 mock-legacy/ | ✅ |

**未验证项（如实登记）**：
- **实景演练计时**：逐字稿按分镜预估时长（≈7 分钟）编写，但**未在目标投影环境完整彩排**——按第 12 章 §七 要求，答辩前至少完整演练一遍并计时；超时按 H1 顺序砍幕。
- 第 6 幕（UNRESOLVED 制造）依赖"查证也查不清"的稳定构造方式（H3），当前以"ADMIN 凭证临时失效后提交"为实现路径，现场稳定性需彩排确认。
- 补偿一幕（D7 连锁）仍按第 12 章一审注维持"不增幕"，补偿链路以第 09 章 D5 实验证据为准；若定稿前决定增补，须按 §六 重审砍幕顺序并登记。

## 四、回滚建议

- 三个脚本与逐字稿均为纯新增演示辅助件，删除即回滚；对引擎与存量系统无依赖关系。
- 实景演练产生的业务数据由 demo-cleanup.sh / 复位脚本清理。
