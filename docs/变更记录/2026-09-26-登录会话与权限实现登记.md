# 2026-09-26 登录、会话与权限实现登记

> **文档性质：变更记录（带日期）**。记录"拓展方案"进入实现期后的**第一件落地物**：登录三件套 + 会话鉴权 + 身份派生 + 意图×角色鉴权。
> **依据**：[展示与能力拓展总纲](../设计方案/2026-09-25-展示与能力拓展总纲.md)（P1 展示是唯一得分项 / P4 说人话）、[登录、身份与权限方案](../设计方案/2026-09-25-登录身份与权限方案.md)、[能力清单与实现规格](../设计方案/2026-09-25-能力清单与实现规格.md) §四 第 1 组、[拓展方案三审](../变更记录/2026-09-25-拓展方案三审-验收标准与拍板销项.md)（总 Gate 关闭 + 各能力开工小 Gate）。
> **为什么先做它**：三审 §八 明确"登录是所有写能力的公共前置，随第 1 组先行"——没有它，C7 查/退、C8 改期、C12 学生占座这三项能力一条也演不了。

---

## 一、交付内容（对照方案的 15 项影响面）

| # | 影响面（方案 §三） | 落地情况 |
|---|---|---|
| 1 | 新增登录/登出/我是谁三个端点 | ✅ `POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`（新文件 `src/access/auth-endpoints.js`）；契约已回写[对外接口清单](../基线文档/对外接口清单.md) §9–§11 |
| 2 | 会话 → 用户 → 角色 → 存量令牌映射 | ✅ `src/access/session-store.js`（引擎自己的会话令牌，内存态）+ `src/contact/user-token-store.js`（登录者本人的存量令牌，按用户名索引） |
| 3 | 意图计划加"所需角色" | ✅ `intent-plans.yaml` 的 `borrow-classroom` 加 `requiredRole: TEACHER` + `roleDeniedMessage` + `roleAlternatives`；`config-store.js` 启动即校验角色合法性（权限是配置数据，不散落在代码里） |
| 4 | 入口处按"意图 × 角色"判权限 | ✅ `src/orchestration/intent-authorizer.js`；结构化入口在 **IDLE 态**、自然语言入口在**理解之后**即落 `REJECTED`，**零调用**（新增状态机事件 `INTENT_FORBIDDEN`，两条出边） |
| 5 | 证据链带发起人与使用身份 | ✅ 每条调用证据带 `initiator`（登录用户）与 `actingIdentity`（本人身份 / 服务只读身份）；`/evidence` 导出同步输出 |
| 6 | 前端登录页与角色标识 | ⛔ **未做**（见 §五）——这是下一件必须做的事，否则前端在新引擎下会全线 401 |
| 7 | 演示脚本新增登录镜头、越权镜头 | ⛔ 未做（演示改版方案已定分镜，待前端就绪后照拍） |
| 8 | 边界文档补充登录与身份 | ⛔ 未做（待这一轮整体收口时一并改） |
| 9 | 对外接口清单登记 | ✅ 已完成（含实际契约、新错误码、实测修订） |
| 10 | SSE 会话鉴权方式 | ✅ 走 Cookie（`EventSource` 不能带自定义头是客观约束）；非浏览器客户端可用 `Authorization: Bearer` |
| 11 | `identity-pool.js` 重构 | ✅ 身份解析从"意图计划写死"改为**服务身份（只读借用）/ 发起人本人（写）**两分：`ContactGateway#resolveIdentity`；服务池仍只持有从环境变量注入的凭证 |
| 12 | `task-store.js` 增加 owner | ✅ 任务记 `owner`；回复/取消/查看/证据导出均校验归属，他人任务一律 `404 + 4041`（不暴露内容） |
| 13 | `evidence-chain.js` 增加 initiator | ✅ 条目新增两个可选字段；`contact-gateway` 每次调用都写 |
| 14 | `identity` 参数 deprecated | ✅ 传了忽略、缺省不再报 `4004`；**140+ 个既有测试批量迁移为"先登录再调用"** |
| 15 | `http-server.js` 会话中间件 | ✅ 除 `/api/health` 与 `POST /api/auth/login` 外全部端点受保护，未登录 `401 + 4010` |

**新增结构性防线一条（方案未写、实测逼出来的）**：入口启动时自检服务身份凭证，缺了就**拒绝启动**并逐条指名缺哪个环境变量。原因是实测发现：缺凭证时引擎照常起来，但**每个任务都以"任务异常终止"收场**——进程像是好的、界面什么都办不成，属于最难排查的一类故障（AGENTS.md"测试全绿不等于能启动"的同类问题）。

---

## 二、G-登录 开工小 Gate：实测结论（三审 §八 要求"不实测不许写"）

实测方式：`orchestrator/scripts/gate-login.mjs`（可复跑；只做登录/登出/查会话，不改业务数据，结束撤销自建设备会话）。原始输出见 §四。

| # | 待验（三审 §八 G-登录 + 能力清单 §六） | **实测结论** |
|---|---|---|
| 1 | 存量登录接口返回结构 | `data = { accessToken, refreshToken, userInfo }`；`userInfo` 含 `role / creditLevel / maxSingleReservationMinutes / dailyReservationLimitMinutes / seatReservationAdvanceHours`；access token 255–257 字符、**默认有效期 30 分钟** |
| 2 | 是否存在刷新令牌接口 | **存在**：`POST /auth/refresh` 可用，**刷新令牌轮换**（返回新 refreshToken），**旧的一次性作废**（再用返回 `code 401 refreshToken 无效或已过期`） |
| 3 | 同一账号重复登录是否会踢掉前一个 | **同 deviceId 重登：旧 access token 仍然可用**（不失效、不占新名额）——录制时重复登录不会打断已在跑的任务 |
| 4 | 设备会话上限与被踢表现 | 上限 3 生效：连续 4 个不同 deviceId 后，**最早的 2 个令牌被淘汰**，被踢后调用返回 **HTTP 401 + 业务码 401「未登录」**；`GET /auth/devices` 只剩 3 条 |
| 5 | 缺 `X-Device-Id` 会怎样 | 登录**照样成功**，deviceId 回落 `unknown-device`——多个无头登录会共享同一设备名额，是隐性坑（已登记注册表 `legacyNotes`） |
| 6 | 密码错误的表现 | **HTTP 200 + 业务码 401「用户名或密码错误」**（注意：不是 HTTP 401）；被踢则是 HTTP 401——两者形态不同，上游映射必须分别处理 |
| 7 | 登出后 access token 是否立即失效 | `POST /auth/logout` → 200；**原 access token 立即 401** |
| 8 | 教师个人时长上限字段 | 教师登录也返回 `maxSingle=360 / daily=720 / seatAdvanceH=24`（**不是 null/0**）→ "教师豁免时长校验"是**业务校验层面的豁免**，不是"字段为空"；C9/C12 的解释话术不能拿字段值当依据，须另找判据（已登记给 G-C9/C12） |

**由此推翻的两条既有文档结论**（诚实登记）：
1. 一/二审结论"存量系统**没有**刷新令牌接口"——**错的**。该结论正是三审"会话内存保存明文密码"的唯一理由；实测既然有刷新令牌且会轮换，实现改为**刷新令牌续期**，密码不落内存。
2. 对外接口清单原写"登录响应含 `user`、无刷新令牌"——已按实际契约修订。

---

## 三、与评审方案的两处偏离（需作者知悉）

| # | 方案原文 | 实现 | 为什么 |
|---|---|---|---|
| 1 | 三审拍板：**会话内存保存密码**用于自动重登（因"无刷新令牌接口"） | **不存密码**，改用刷新令牌续期一次；续期失败 → `4011 登录已失效，请重新登录` | 实测刷新令牌存在且轮换。存明文密码是"为了稳定性牺牲安全"，而真实约束并不存在——按 P1（展示是唯一得分项）也更好讲："我们不存你的密码" |
| 2 | 决定 6 提"令牌过期自动重登" | 会话续期 = 刷新令牌；**单飞**（同一用户并发续期只发一次刷新请求），避免轮换令牌被并发用掉 | 轮换令牌一次性作废，并发刷新会让后到者拿到失效令牌 |

---

## 四、验证结果（如实登记）

| 层 | 命令 | 结果 |
|---|---|---|
| 结构自检 | `npm run check` | **8/8 通过** + 约束检查 **4/4 通过** |
| 测试 | `npm test` | **148/148 通过**（既有 140 个全部迁移为"先登录再调用" + 新增 8 个：登录链路/未登录 4010/Bearer 会话/每条证据带发起人/`identity` 已作废/越权拒绝零调用/任务归属/会话失效） |
| 进程真的能起 | `node src/access/main.js`（8091） | ✅ 起得来；**且缺凭证时会 fail-fast**（退出码非 0，逐条指名缺哪个变量） |
| 真实链路冒烟 | `powershell -File deploy/scripts/engine-smoke.ps1`（新增，Windows 原生） | **全部通过**：健康检查（含 `chaos.armed`）→ 未登录 401 → 登录（TEACHER）→ `me` → 只读意图全链路 **DONE**（结论"该教室在这个时段可以使用"）→ 任务 owner=登录用户 → 证据链 5 条调用全带发起人、身份为"本人身份 / 服务只读身份" → 登出后 401 |

**冒烟抓到并已修复的缺陷 1 处**：引擎缺服务身份凭证时不报错、任务静默变成"任务异常终止" → 改为启动即失败（见 §一 末条）。

---

## 五、仍未做（不得假装已完成）

1. ~~前端登录页与会话~~ → **已完成**，见 §八。
2. ~~演示检查清单适配登录~~ → **已完成**，见 §八（顺带修掉脚本原有多处缺陷）。
3. 其余 13 项能力（C7/C8/C12 及其余两组）未开工；`identity` 参数与 `4004` 的正式移除未做（三审：本轮不删）。
4. C14 两段式确认、`POST /api/notifications/read-all` 专用端点未做（待各自小 Gate）。
5. `AGENTS.md` 的"当前阶段"、边界文档 §四 速查表、演示改版方案的录制 SOP 待随本轮收口一起更新。

---

## 八、前端适配与演示路径修复（同日续做，均已实测）

### 8.1 前端（`frontend/`）

| 文件 | 改动 |
|---|---|
| `src/api/client.js` | 新增 `apiLogin/apiLogout/apiMe`；所有请求显式 `credentials: 'same-origin'`（同源代理携带会话 Cookie）；**去掉 `identity` 参数** |
| `src/stores/session.js` | **新增**：会话状态（unknown/checking/anonymous/authenticated）、`restore()`（重开窗口恢复会话）、`login/logout`、`markExpired()` 人话提示；角色中文名映射（教师/学生/管理员） |
| `src/views/LoginView.vue` | **新增**：登录页（账号/密码 + 错误与"登录已过期"提示 + 演示账号名提示）；口令不在页面留存 |
| `src/App.vue` | 未登录不进主界面；顶栏加**角色徽章 + 昵称 + 退出登录**；退出时清空上一人的任务视图 |
| `src/stores/task.js` | 去掉硬编码 `identity`；401 统一走 `markExpired()` 退回登录页（不假装还登录着） |

验证：`npm run build` 通过（46 模块）；**经 5173 代理实测**——登录 200（role=TEACHER、下发 1 个会话 Cookie）→ 带 Cookie `/api/auth/me` 200 → 不带 Cookie 401。
（未做浏览器端交互录制验证：本轮由作者手动确认；脚本层证据见下。）

### 8.2 演示路径（`deploy/scripts/`）

- **`demo-checklist.sh`**：新增第 3b 项"引擎会话"（登录 200 / 带会话 200 / 不带会话 401）；第 6 项混沌撤防由"人工提醒"改为**机械断言** `chaos.armed=false`（三审 P2-2）；`/api/chat`、`/api/tasks` 改走会话。
- **`lib/legacy-credentials.sh`（新增）**：演示账号口令"**环境变量优先，缺失回落 `start-all.bat` 的演示默认值**"（与 node 侧 `legacy-credentials.mjs` 同口径）。理由：要求人先 export 三个口令既容易忘、又会把口令写进命令历史。
- **顺带修掉清单原有 4 处缺陷**（此前每跑必红的根因，不是环境噪声）：
  1. 对存量系统的只读检查（`/admin/reservations`、`/classrooms/{id}`、`/admin/maintenance`）**全都没有带令牌** → 一律 401；
  2. 三个身份"探活"每个身份**登录两次**（多余的设备会话占用，与 §九 9.3 的顺序纪律相冲）→ 改为各登录一次并复用令牌；
  3. 多处用 **node 解析 JSON**，而脚本运行时 PATH 里不一定有 node → 全部静默返回空、看起来像业务失败 → 改为 grep/tr 粗判；
  4. 第 13 项用 `../orchestrator/...` 相对 CWD 取值，从仓库根跑必然失败 → 改为按脚本位置推导仓库根。

**实测结果（无任何环境变量、直接 `bash deploy/scripts/demo-checklist.sh`）**：**通过 18 / 失败 0**（含 1 项人工目视）。

---

## 六、踩坑登记（写给后来的自己）

1. **Gate 脚本死循环**：`gate-login.mjs` 清理段"遍历 `createdDevices` 的同时又往该数组 push"，`for...of` 边遍历边追加 → 永不结束，输出文件涨到 1.8 MB、连滚 40 分钟，只能人工杀进程。**已修**（遍历快照 + 登录时留存 token 不再重登），并给脚本加了**全局看门狗**（默认 90 秒硬超时、打印"卡在哪一步"、非零退出）与**逐步骤打印**。纪律：跑脚本一律带超时包装，不允许无声长跑。
2. **Windows PowerShell 5.1 读 `.ps1` 按 ANSI 解**：无 BOM 的 UTF-8 脚本里中文注释会变乱码并导致语法错误。新增的 `engine-smoke.ps1` 已加 UTF-8 BOM；以后加 `.ps1` 必须带 BOM。
3. **抓凭证的正则别按裸 `NAME=` 匹配**：`start-all.bat` 顶部注释里也出现 `ORCH_LEGACY_ADMIN_PASSWORD=...` 的说明文字，裸匹配会先命中注释、取到说明文字而不是口令（实测因此得出"管理员登录失败"的错误结论）。已收紧为只认 `set "NAME=值"`。

---

## 七、回滚方式

本轮改动集中在 `orchestrator/`（新增 7 个文件、修改 18 个）+ `deploy/scripts/engine-smoke.ps1` + 本文与接口清单。
- 若要退回"无登录"形态：还原 `src/access/http-server.js`、`src/contact/contact-gateway.js`、`src/orchestration/task-runner.js` 三个文件，并删除 5 个新增模块与 `INTENT_FORBIDDEN` 两条出边（状态机自检 8 条会在缺边时报错，正好挡住半退状态）。
- 项目**未提交任何 commit**；本轮改动全部在工作区，`git checkout -- orchestrator/` 可整体丢弃。
