# 参数标定与对照实验报告（阶段收尾 · 三审 §七 遗留标定项）

> **文档性质：实验记录（docs/实验，可复跑）。执行器：`orchestrator/scripts/run-calibrations.mjs`；样本集：`orchestrator/scripts/calibration/e6-samples.mjs`；原始数据：[2026-09-25-参数标定与对照实验-原始数据.json](./2026-09-25-参数标定与对照实验-原始数据.json)。**
> 运行时刻：2026-09-25；模型：qwen-plus（E1/E6 部分）；路径：只读真实调用 + 真实 LLM。

---

## B3 · 调用超时复标（决策 B3）

| 接口 | n | min | p50 | p95 | max | avg |
|---|---|---|---|---|---|---|
| 轻量读（GET /classrooms/5） | 50 | 2ms | 3ms | 4ms | 126ms | 5ms |
| 全量列表（GET /admin/reservations） | 10 | 9ms | 11ms | 80ms | 80ms | 17ms |

**结论**：现行超时（写 3000ms / 读 3000ms / 重列表 5000ms）对实测最大值有 **38×–63×** 余量——**初值维持，无需调整**。

## C16 · 证据片段 500 字符截断验证（决策 C16）

| 响应 | 字节长 | 前 500 字符含 "code" |
|---|---|---|
| classroom.detail | 154 | ✓ |
| admin.reservations（86+ 行） | 63,217 | ✓ |
| maintenance（空） | 582 | ✓ |

**结论**：500 字符截断下 envelope（code/message）均可见——截断不影响故障诊断。**维持 500**。

## E6/D7 · 提示词含/不含接口信息对照（决策 E6；F3 样本量定案 = 30 条）

样本集（`scripts/calibration/e6-samples.mjs`）：30 条 = 借教室 8 + 查询 6 + 槽位不全 6 + 意图多义 4（不评分意图）+ 超域 6。

| 版本 | schema 通过 | 意图准确 | 超域正确 |
|---|---|---|---|
| **不含接口信息（现行）** | 28/30 | **20/20** | 26/26 |
| 含接口信息（对照） | 28/30 | **20/20** | 26/26 |

- 两版各 2 条 schema 违规（flexible 类：裸名词"数智楼222"等，模型输出未过 JSON 校验）——**两版完全一致**。
- **结论：第 03 章 §六 假设成立（未被证伪）——提示词不含接口信息，准确率不下降。** 接口信息版同样无提升，即接口清单对理解层无增益，维持"零接口信息"纪律。

## E1 · 置信度阈值标定（决策 E1；现行 0.55）

意图正确的 20 条样本置信度：min 0.65 / p50 0.93 / max 0.95——**全部 ≥ 0.55**；意图错误样本本轮为 0（无数据点）。

**结论**：**阈值 0.55 可用**（正确样本全过、无误拦）；错误样本零散未构成本轮标定依据，若后续演示中出现误拦再复标。

## H3 · 稳定"查不清"构造验证（决策 H3，第 6 幕前置）

**构造**：T5 注入 create 第 1 次（真实转发落库后丢响应）+ T1 阻断查证主路径 logi.list 第 2–8 次（含 R9 内部重试）+ T1 阻断降级 mine 第 2–6 次 → 首选与降级路径均 R10 事实不可得 → **INCONCLUSIVE → UNRESOLVED**。

3 次复跑全部 UNRESOLVED——**构造稳定，第 6 幕可用**。首查不清的实质：查证的三条路径（管理端全量 / mine / 座位占用）中，前两条被阻断、第三条（座位占用）在 F3 已由命题层判定为歧义来源——与第 06 章 §五"三条路径极限"的推演一致。

## 遗留

- E1 的错误样本本轮为 0，阈值下界（0.55）的实际拦截场景未在标定中复现——演示/彩排中如遇误拦再复标。
- F3 样本集 30 条已定案入库（），后续增补直接改样本文件重跑。
- 实验产生的 ACTIVE 记录已全部撤销（ACTIVE=0 复核通过）。
